name: Stabilize JINSHIUAN homepage

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Homepage is stabilized; no automatic repair is required."
